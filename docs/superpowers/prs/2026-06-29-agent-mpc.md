
---

## Post-merge cherry-pick

After this PR was opened, subproject 6's `/api/v1/forecast/item/<id>`
was updated to use the same "consumption" source as `/forecast` and
`/inventory` (preview-phase1 commit 1e7f2ba). The inlined
outbound-only SQL in `blueprints/agent_mpc.py::forecast_item` was
replaced with a call to `blueprints.consumption.fetch_item_movements_30d`,
the shared source-of-truth helper. The updated branch includes this
fix as a follow-up commit:
`fix(mpc): use shared consumption helper for /api/v1/forecast/item
(cherry-pick from preview-phase1)`.
